{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "theme": "auto",
  "skipDangerousModePermissionPrompt": true,
  "statusLine": {
    "type": "command",
    "command": "cols=$(stty size </dev/tty 2>/dev/null | awk '{print $2}'); export COLUMNS=$(( ${cols:-120} > 4 ? ${cols:-120} - 4 : 1 )); plugin_dir=$(ls -1d \"${CLAUDE_CONFIG_DIR:-$HOME/.claude}\"/plugins/cache/*/claude-hud/*/ 2>/dev/null | sort -V | tail -1); if [ -z \"$plugin_dir\" ]; then printf ''; else exec /usr/local/bin/node \"${plugin_dir}dist/index.js\"; fi",
    "padding": 0
  },
  "includeCoAuthoredBy": true,
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "mirabilis hook session-start",
            "timeout": 60
          }
        ]
      }
    ],
    "PostToolUseFailure": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "mirabilis hook post-tool-use-failure",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
